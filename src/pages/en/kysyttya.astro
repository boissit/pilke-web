---
/* The Finnish slug, as with the other two subpages, so a language switch is the
   prefix and nothing else. */
import FaqPage from '../../components/FaqPage.astro';
import Base from '../../layouts/Base.astro';
---

<Base lang="en"><FaqPage lang="en" /></Base>
