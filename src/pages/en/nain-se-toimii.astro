---
/* The Finnish slug, as with the safety page, so a language switch is the prefix
   and nothing else. */
import DetailPage from '../../components/DetailPage.astro';
import Base from '../../layouts/Base.astro';
---

<Base lang="en"><DetailPage lang="en" /></Base>
